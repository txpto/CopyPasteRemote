__pycache__/
*.py[cod]
.venv/
.env
data/

# Local deployment kit (RUNBOOK + setup scripts). Contains site-specific data
# (public IP, internal topology) — keep it local, never publish to this repo.
/deploy/
