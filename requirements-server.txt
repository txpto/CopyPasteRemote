# CopyPasteRemote orchestrator (server) dependencies.
# The server normally runs on a modern Linux/Windows VM, so recent versions are fine.
# Python 3.8+ supported.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
pydantic>=2.5,<3
cryptography>=42.0
python-multipart>=0.0.9
